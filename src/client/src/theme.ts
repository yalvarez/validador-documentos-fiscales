import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#197600',
    },
    secondary: {
      main: '#9c27b0',
    },
  },
});

export default theme;
